const { newsArticles, categoryArticles, userActivity } = require("./InitialMockData");
const { addNewData, saveMockData } = require("./utils");

module.exports = { newsArticles, categoryArticles, userActivity, addNewData, saveMockData };
